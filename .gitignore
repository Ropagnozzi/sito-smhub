# CNAME: resta fuori finché il sito è in staging, come su sito-diessemedia.
# Quando si va live con il dominio, si toglie questa riga e si pubblica il file.
CNAME

Thumbs.db
desktop.ini

# File temporanei di Office (lock quando un file è aperto)
~$*

# Foglio di revisione: si rigenera con fai-controllo-tipologie.py, non va pubblicato.
controllo-tipologie.html
lockup-payoff.html
prova-hero.html

# Originali delle foto verticali (pesanti): nel sito vanno le copie in hero-mobile/.
assets/foto/mobile/*
!assets/foto/mobile/LEGGIMI.txt
# Promemoria di quale originale ha generato ogni copia: servono solo in locale.
assets/foto/hero-mobile/*.da

# La presentazione originale resta in locale: sul sito vanno le foto compresse.
SMHUB Domination*.pdf
